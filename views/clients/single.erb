<%=

@header = @client.name
@facts = {
  'Address'  => @client.address,
  'City'     => @client.city,
  'Postcode' => @client.postcode,
  'Jobs'     => @client.projects.count,
  'Sessions' => @client.sessions.count
}

erb :"partials/detail"
%>
