<%=
  @cards = @clients
  @header = lambda(&:name)

  @meta = lambda do |client|
    "#{pluralize client.jobs.count, 'jobs'} and #{pluralize client.sessions.count, 'sessions'}"
  end

  erb :"partials/cards"
 %>
