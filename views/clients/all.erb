<%=
  @cards = @clients
  @header = lambda(&:name)

  @meta = lambda do |client|
    "#{pluralize client.projects.count, 'projects'} and #{pluralize client.sessions.count, 'sessions'}"
  end

  erb :"partials/cards"
 %>
