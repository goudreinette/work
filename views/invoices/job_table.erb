<table class="ui celled table job-table">
  <thead>
    <tr>
      <th>Job</th>
      <th>Subtotal</th>
      <th>Tax 21%</th>
      <th>Total</th>
    </tr>
  </thead>

  <tbody>
    <% @jobs.each do |job| %>
      <tr>
        <td><%= job.name %></td>
        <td><%= money job.cost  %></td>
        <td><%= money job.tax %></td>
        <td><%= money job.cost_with_tax %></td>
      </tr>
    <% end %>
  </tbody>

  <tfoot>
    <tr>
      <th></th>
      <th><%= money @invoice.subtotal %></th>
      <th><%= money @invoice.tax %></th>
      <th><%= money @invoice.total %></th>
    </tr>
  </tfoot>
</table>
