<%=
@header = "#{@session.project.name} #{session_span @session}"
@facts  = {'Job'  => @session.project.name,
           'Description'   => @session.description || '-',
           'Paid session?' => @session.paid? ? 'Yes' : 'No',
           'Duration'      => @session.duration}

erb :"partials/detail"
%>
